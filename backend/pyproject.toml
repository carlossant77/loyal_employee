[project]
name = "exo-agente-ci-sonar-backend"
version = "0.1.0"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30", "pydantic>=2.8", "libsql-client>=0.3"]

[project.optional-dependencies]
test = ["pytest>=8.0", "httpx>=0.27"]

[tool.pytest.ini_options]
pythonpath = ["."]
